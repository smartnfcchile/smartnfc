import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getPost, posts } from "../../../lib/blog";

export function generateStaticParams(){return posts.map(({slug})=>({slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const post=getPost(slug);if(!post)return{};return{title:post.title,description:post.description,alternates:{canonical:`/blog/${slug}`},openGraph:{title:post.title,description:post.description,type:"article",publishedTime:post.published}}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=getPost(slug);if(!post)notFound();const jsonLd={"@context":"https://schema.org","@type":"BlogPosting",headline:post.title,description:post.description,datePublished:post.published,dateModified:post.published,inLanguage:"es-CL",author:{"@type":"Organization",name:"SmartNFC"},publisher:{"@type":"Organization",name:"SmartNFC",url:"https://smartnfc.cl"},mainEntityOfPage:`https://smartnfc.cl/blog/${post.slug}`};return <main className="article-page"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd).replace(/</g,"\\u003c")}}/><nav className="content-nav"><Link href="/" className="wordmark">Smart<span>NFC</span></Link><Link href="/blog">Todos los recursos</Link></nav><article><header><span>{post.category.toUpperCase()} · {post.readTime.toUpperCase()}</span><h1>{post.title}</h1><p>{post.description}</p><time dateTime={post.published}>14 de agosto de 2026</time></header><div className="article-body"><p className="article-intro">{post.intro}</p>{post.sections.map((section)=><section key={section.heading}><h2>{section.heading}</h2>{section.paragraphs.map((paragraph)=><p key={paragraph}>{paragraph}</p>)}</section>)}<aside><b>¿Quieres verlo aplicado a tu equipo?</b><p>Conoce cómo SmartNFC combina tarjetas NFC, perfiles digitales y seguimiento comercial.</p><a href="https://wa.me/56944891518?text=Hola%2C%20quiero%20una%20demostraci%C3%B3n%20de%20SmartNFC.">Solicitar demostración</a></aside></div></article></main>}
